{{ form_start(formVente, {'attr': {'onsubmit': 'return handleForm(event);'}}) }}
    {{ form_widget(formVente) }}
<button class="btn">
    {{ button_label|default('Enregistrer') }}
</button>
{{ form_end(formVente) }}
