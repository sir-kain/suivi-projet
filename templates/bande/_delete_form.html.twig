<form method="post" action="{{ path('app_bande_delete', {'id': bande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bande.id) }}">
    <button class="btn" data-tooltip="Supprimer cette bande">Delete</button>
</form>
